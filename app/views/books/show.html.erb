<%= @book.title %>

<% @book.reviews.each do |review| %>
  <%= review.body %>
  user name:<%= review.user.name %>
  rating:<%= review.rating %>
  <% end  %>
  average rating:<%= @book.average_rating %>
  highest rating:<%= @book.average_rating %>
  lowest_rating rating:<%= @book.average_rating %>
